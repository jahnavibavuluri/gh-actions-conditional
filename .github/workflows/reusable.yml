name: reusable deploy 
on: workflow_call # this allows this workflow to be called from other workflows 
jobs:
    deploy:
        runs-on: ubuntu-latest 
        steps:
            - name: output information 
              run: echo "deploying & uploading..."
